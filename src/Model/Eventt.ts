export type Eventt = {
  id: string;
  name: string;
  date: string;
  category: string;
  imageUrl?: string;
  url?: string;
  info?: string;
  location?: string;
  description?: string;
  latitude: number;
  longitude: number;
};
